import { SvgTooltipDirective } from './svg-tooltip.directive';

describe('SvgTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
